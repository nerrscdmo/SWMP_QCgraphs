1. Pattern matching - to make sure scripts only run on the proper files (then can remove lines about the files not being error-proofed, and how only the right kind of files can be in the directory)  
    -  incorporated in WQ loop file, starting line 33
2. See if there's a better way to interactively choose a directory nowadays  
    -  rstudioapi - see dev/pattern_matching.R
3. FILE PATHS - e.g. line 45 in WQ QC looping script - don't paste these; use a file path constructor function, because different operating systems work differently
    -  incorporated in WQ script, lines 45 & 51  (input and output files)  
4. Might be able to use lubridate to parse date-times, rather than the complex logic I developed way back when. See e.g. lines starting at 57 in WQ looping script.  
    -  see `parse_date_time()` - can specify multiple possible orders (in examples, look at **heterogeneous date-times**)  
    -  looks like I was originally worried about `mdY HM` vs. `Ymd HM`
5. Date-times are using America/Regina time zone! Do something about this?  
    -  .  
6. Sometimes the DateTime column is named "TIMESTAMP" (in wkb example)  
    -  WAIT THIS IS MET!  
7. See if Jeremy or someone will give these a test run once they're updated  